export const darkMode = "class";
export const content = [
  "./index.html",
  "./src/**/*.{js,jsx,ts,tsx}", // Include all files where you use Tailwind
];
export const theme = {
  extend: {
    // You can extend Tailwind with custom colors, fonts, etc.
  },
};
export const plugins = [];
